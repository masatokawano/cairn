"""Cross-source recall: related() and weekly digest generation.

Populated by M4 (DESIGN.md §5.3). At M0 the module skeleton is present but
empty; the implementation depends on M1 (items registry populated for
karakeep/zotero) and M3 (obsidian). Adding functions here at M0 would be a
先回り実装 violation.
"""
