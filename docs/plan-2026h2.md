# Cairn 実行計画 2026 下半期

- 作成日: 2026-07-12
- 位置づけ: 今想定されている開発の**具体的な次のステップ**を順序・完了条件・
  承認要件まで落とした実行計画。正典は `docs/DESIGN.md`（Cairn 本体）と
  `docs/adr/0005-personal-health-observatory.md` + `docs/health/`（健康ドメイン）。
  本計画と正典が食い違ったら正典が正。長期方向は `docs/NORTH_STAR.md`、
  思弁的地平は `docs/HORIZONS.md`。
- 完了した項目は本文書を更新して ✅ を付ける（backlog.md と同じ規律）。

## 0. 現在地（2026-07-12）

- **Cairn 本体**: M0〜M6①② 完了。M6③ 実運用評価の期間中（W29〜W31、〜7月下旬）。
  A0 データ穴埋め完了 — S5 の 4 系統すべてに実データあり、embedding バックログ解消済み。
- **健康ドメイン**: H0〜H4 実装済み + 実データ検証済み（検査 729 観測 / Apple Health
  57 万観測、単一タイムライン）。実機 store は schema v3。
- **凍結の意味**: M6③ は「機能追加をしない評価期間」。健康ドメインの cairn.db
  非接触部分は凍結対象外（オーナー決定 2026-07-11）。Cairn 統合に触れる H5 以降は
  凍結明けが前提。

## 1. トラック P0 — いま進行中（凍結中も可、主に人間の手順）

| # | 作業 | 担当 | 状態 |
|---|---|---|---|
| P0-1 | **R2 評価記録**: 毎週日曜、W29〜W31 の 3 回。読了分数 / 再発見○× / ノイズ件数 / MCP 使用回数 / failures.log 確認（5分、status-2026-07-06.md §4 R2） | 人間 | W29 から |
| P0-2 | 服薬・サプリ・生活イベントの実データ YAML 作成 → `cairn health import events`（書式は health/DESIGN.md §5.3）。入れば event-response が実データで動く | 人間（書式支援はエージェント） | 未 |
| P0-3 | 実medical文書の登録試行（`import document` + attach-text の実運用確認） | 人間 | 任意 |
| P0-4 | Apple Health の月次 export 取り込み（当面手動。自動リマインダは P5 で） | 人間 | 運用開始 |

## 2. トラック P1 — 凍結解除ゲート（7月下旬）

1. W31 終了時点で S1〜S5 を判定（3 週分の記録に基づく）。
2. 調整は**件数上限と related チューニングのみ**（B1〜B3、DESIGN.md §7 M6 の規定）。
   - B1: build_context_pack evidence バケットの相対スコア閾値（空バケット許容）
   - B2: related() semantic アームの距離閾値・ノイズタイトル規則
   - B3: 件数上限（各節 ≤10 の見直し）
3. 判定と調整の記録を status doc 形式で残し、backlog の凍結条項を解除に書き換える。
   **これが M6 完了 = DESIGN.md §7 の全マイルストーン完了**。ROADMAP.md 状態表を更新。

完了条件: S1〜S5 判定文書 + チューニング反映 + 文書更新。

## 3. トラック P2 — H5: レポートの Obsidian 配信（凍結明け第1弾）

セキュリティ境界の変更を含むため、このトラックのみ**着手前に計画レビュー、
実装後に独立レビュー（Codex）**を必須とする。

| 順 | 作業 | 内容 |
|---|---|---|
| 1 | H5-P1 最終決定 | Vault 複製の扱い（PRIVACY.md §10）。既定=全同期除外は適用済み（.stignore）。opt-in するレポート種別（候補: next-visit-brief のみ）を決めて PRIVACY.md に記録。Remotely Save 導入済み端末では skip 設定確認 |
| 2 | allowlist 第4カテゴリ | `obsidian_writer.py` に `"health": ("90 Auto/Health", overwrite=True)` を追加。パス検証・symlink 拒否テストを health カテゴリにも拡張 |
| 3 | AGENTS.md 不変条件2 改訂 | 「allowlist 3箇所」→ 4箇所。同一コミットで |
| 4 | 配信実装 | current-status / health-timeline / lab-trends / data-quality を `90 Auto/Health/` へ（上書き可）。解釈草案は `00 Inbox/AI Drafts/`（新規のみ）。ACCEPTANCE H5 の 11 項目が完了条件 |
| 5 | 独立レビュー | 不変条件 1〜9・allowlist・エスケープ・H5-P1 準拠を確認 |

規模: S〜M。前提: P1 完了。

## 4. トラック P3 — H6: AI 解釈と改訂履歴

- interpretations / interpretation_evidence / data_snapshots テーブル（schema v4）
- ollama（D10: qwen 系ローカル既定）で bounded evidence set から解釈 draft 生成。
  model_id / prompt_version / snapshot / limitations / confidence を必ず記録
- accepted / rejected / superseded の状態遷移（accepted は人間のみ）。
  evidence なしの解釈は accepted にできない（ACCEPTANCE H6）
- 安全語彙テスト: 診断・薬剤変更を自動決定として提示する出力の拒否
- **副産物**: HORIZONS 1.3 供養録（rejected/superseded の年次一覧）はここでほぼ無償

完了条件: ACCEPTANCE H6 の 10 項目。規模: M。独立レビュー推奨（プロンプト境界 +
untrusted fence の確認）。

## 5. トラック P4 — H7: health MCP（opt-in・bounded）

- ツール候補: health_get_current_status / get_timeline / query_observations /
  compare_periods / get_event_response / build_visit_brief / get_interpretation_history
- 既定無効。metric・期間・集計粒度・行数上限を強制。原文と合成の構造分離
- 完了条件: ACCEPTANCE H7 の 7 項目。規模: M。独立レビュー必須（新しい開示面）

## 6. トラック P5 — H8: 長期運用

- 暗号化バックアップ + restore テスト、保持ポリシー、削除手順（不変条件8準拠の列挙式）
- premigrate バックアップと raw/ のローテーション
- import inbox + 失敗通知（既存 CAIRN_NOTIFY 機構の再利用）、Apple Health 月次
  export リマインダ（launchd 通知、iOS アプリは作らない）
- 完了条件: ACCEPTANCE H8 の 7 項目。規模: S〜M

## 7. トラック P6 — 受診ループの完成（ROADMAP H10 として追記提案）

H5/H6 完了後。着手時に docs/health/ROADMAP.md へ H10 を正式追記（Decision 不要、
ROADMAP 拡張のみ。ADR-0005 の範囲内）:

1. **verified 抽出テキスト → observations 昇格**: H4 の verified ライフサイクルの先。
   検証済みテキストから構造化観測値を取り込む入口（CSV 手動転記の置き換え）
2. **質問キューの追跡**: visit-brief に載せた質問 → 医師の回答（interpretation,
   author=clinician）→ 解決/未解決の状態管理。unresolved questions レポートの発展
3. ループ: brief 生成 → 通院 → 説明を記録 → 検査票を document 登録 → verified 化 →
   観測へ昇格 → 次回 brief に反映

規模: M。完了条件は追記時に ACCEPTANCE H10 として定義。

## 8. トラック P7 — Re-validation runs（ROADMAP H11 候補）

H6 の interpretations（model_id / prompt_version / snapshot 付き append-only）を使い、
**モデル世代交代時に新モデルが過去の AI 解釈を evidence ごと再点検し、
支持/弱化/反証/未解決の判定と supersede 案を draft で出す**。採否は人間。

- NORTH_STAR §6・§10（モデル置換後も意味を失わない）の直接実装
- 汎用 validation primitive の最有力候補 → H9（汎用化評価）の主要材料
- 規模: M。前提: H6 + 実運用で解釈が数十件たまってから

## 9. Seeds — 凍結・依存と無関係にいつでも

| Seed | 内容 | 規模 |
|---|---|---|
| S-1 予測台帳 | HORIZONS 1.1。events YAML パターン流用の最小実装（主張・確信度・期日・結果・照合）。health とは別 namespace で汎用 | XS〜S |
| S-2 原本汚染への防衛線 | HORIZONS 2.3。エージェントは原本領域に書かない・AI 生成物は常にラベル付き分離、を AGENTS.md 恒久条項化（文書のみ） | XS |

## 10. Cairn 本体の凍結明け候補（C 系、優先順）

| # | 候補 | 前提 |
|---|---|---|
| C1 | 定期 reindex エージェント（深夜 1 回 `index rebuild`） | **先に DESIGN.md §5.7 の 1 行改訂**（エージェント 2 本規定との整合）。plist load は不変条件 8 |
| C2 | `/api/healthz` | なし。XS |
| C4 | CI（pytest + 依存監査 + frontend build） | なし。S。health テスト 88 件も回る |
| C7 | 複数 Mac の claude/codex ログ集約（端末 ID 付き staging） | M。設計提案から |

## 11. 実施順序の全体像

```
7月         P0（評価記録・実データ運用）──────────────┐
7月下旬     P1 凍結解除ゲート（S1〜S5 判定 + B1〜B3）  │ Seeds S-1/S-2 は
8月         P2 H5 配信（レビュー付き）                 │ どこでも挿入可
8〜9月      P3 H6 解釈 → P4 H7 MCP（レビュー付き）     │
9〜10月     P5 H8 運用 + C1/C2/C4                      │
10月〜      P6 受診ループ（H10）                       │
モデル世代交代時  P7 re-validation（H11）              │
年末        H9 汎用化評価 + HORIZONS 1.2 belief diff の初回（年次スナップショット）
```

判断の物差し（HORIZONS §4 と同じ）: **検証ループを完成させるものが先、
生成を増やすだけのものは後。**
